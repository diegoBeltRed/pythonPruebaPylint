name: Pylint

on: [push]


jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.8", "3.9", "3.10"]
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pylint
        pip install radon
        pip install lizard

    - name: Analysing the code with pylint
      run: |
        mkdir -p docs/pylint
        pylint --exit-zero --output-format=json:docs/pylint/report.json $(git ls-files '*.py')

    - name: Analysing the code with radon
      run: |
        mkdir -p docs/pylint
        radon raw $(git ls-files '*.py') -j > docs/pylint/raw.json
        radon cc $(git ls-files '*.py') -j > docs/pylint/radon.json
        radon mi $(git ls-files '*.py') -j > docs/pylint/mi.json

    - name: Analysing the code with lizard
      run: |
        mkdir -p docs/pylint
        lizard $(git ls-files '*.py') -xml > docs/pylint/lizard.xml

    - name: Commit report
      if: matrix.python-version == '3.10'
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

        git add docs/pylint/report.json
        git add docs/pylint/radon.json
        git add docs/pylint/raw.json
        git add docs/pylint/mi.json
        git add docs/pylint/lizard.xml 

        git commit -m "Pylint Report" || true
        git push 

    
    
